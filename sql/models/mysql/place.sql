create table place (
	id integer auto_increment primary key,
	attributes varchar(250),
	country varchar(50),
	country_code varchar(3),
	full_name varchar(100),
	id_places varchar(250),
	name varchar(250),
	place_type varchar(50),
	url varchar(250)
);